Private packages is where you store common code for your suite of apps.

Prime candidates include shared Collections, global access control, schema definitions, shared templates, shared 3rd party packages.